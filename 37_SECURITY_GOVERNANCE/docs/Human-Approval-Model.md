# Human Approval Model

Changes follow:

```text
Propose -> Review -> Approve -> Execute (future capability)
```

The baseline v1.6.0 does not enable direct execution. An approval ID is an auditable reference; it is not proof that the requested action is safe or appropriate by itself.
