type: object
required: [provider, path, purpose]
properties:
  provider: {type: string}
  path: {type: string}
  version: {type: [string, integer]}
  purpose: {type: string}
  ttl_seconds: {type: integer, minimum: 1}
