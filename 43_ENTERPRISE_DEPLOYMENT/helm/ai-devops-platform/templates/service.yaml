apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-devops.fullname" . }}
  labels:
    {{- include "ai-devops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: ai-devops-runtime
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
