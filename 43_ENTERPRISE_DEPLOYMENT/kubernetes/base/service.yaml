apiVersion: v1
kind: Service
metadata:
  name: ai-devops-runtime
  namespace: ai-devops
spec:
  selector:
    app.kubernetes.io/name: ai-devops-runtime
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
