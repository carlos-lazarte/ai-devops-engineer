# Context Intelligence

The context planner combines lexical relevance, graph proximity and Skill candidates into a deterministic context packet.

```bash
python 48_KNOWLEDGE_GRAPH_CONTEXT_INTELLIGENCE/cli/contextctl.py plan --query "Kubernetes node not ready" --root .
```
