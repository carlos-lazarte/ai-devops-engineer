# Graph Index

The graph index is generated from the Vault and is safe to delete and rebuild.

```bash
python 48_KNOWLEDGE_GRAPH_CONTEXT_INTELLIGENCE/graph/build_graph.py --root . --output .knowledge-graph/graph.json
```
