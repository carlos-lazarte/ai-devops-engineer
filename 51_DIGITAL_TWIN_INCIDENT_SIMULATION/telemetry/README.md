# Synthetic Telemetry

The twin emits deterministic synthetic metrics and logs.

Example:
```text
kube_node_status_condition{condition=NotReady}=1
kubelet reports node condition NotReady
```

These values are simulation fixtures, not production telemetry.
