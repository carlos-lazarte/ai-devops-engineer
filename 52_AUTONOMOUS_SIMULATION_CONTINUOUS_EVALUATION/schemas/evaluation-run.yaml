type: object
required: [run_id, candidate_id, scenarios]
properties:
  run_id: {type: string}
  candidate_id: {type: string}
  scenarios:
    type: array
    items: {type: string}
  baseline_id: {type: string}
  status: {type: string}
