# Cost and Latency Trade-offs

Routing can consider estimated cost and latency, but these are not substitutes for quality or safety.

For critical DevOps tasks:
- quality tier is a hard constraint
- safety remains an independent gate
- cost/latency are tie-breakers or eligibility constraints

The reference registry uses estimates only; real provider billing and runtime latency require measurements in the target environment.
