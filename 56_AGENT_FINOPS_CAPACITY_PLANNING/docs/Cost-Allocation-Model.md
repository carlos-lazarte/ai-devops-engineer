# Cost Allocation Model

Reference allocation dimensions:

```text
tenant
environment
model
skill
task class
```

A cost event should retain its provenance:

```text
provider_reported
locally_metered
estimated
```

Allocation is accounting metadata. It does not imply authorization or chargeback policy unless the enterprise explicitly adopts it.
