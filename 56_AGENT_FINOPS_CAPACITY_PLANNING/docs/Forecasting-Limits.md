# Forecasting Limits

A forecast is an estimate, not a guarantee.

The reference method is intentionally simple and deterministic. It should be replaced by a statistically validated forecasting implementation when production history is available.

Do not use forecast values to silently raise production budgets.
