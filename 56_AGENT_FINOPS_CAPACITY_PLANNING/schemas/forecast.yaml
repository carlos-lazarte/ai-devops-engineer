type: object
required: [scope, horizon, forecast_cost]
properties:
  scope:
    type: object
  horizon: {type: string}
  forecast_cost: {type: number}
  forecast_tokens: {type: integer}
  confidence: {type: number}
  method: {type: string}
