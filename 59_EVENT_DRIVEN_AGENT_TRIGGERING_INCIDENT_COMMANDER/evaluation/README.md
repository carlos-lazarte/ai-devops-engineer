# v3.7 Evaluation

The evaluation set tests event-driven trigger eligibility, human gating, tenant isolation, approval scope and planner handoff.

Ground truth belongs outside the agent context. It is used only by the evaluator to calculate metrics.
