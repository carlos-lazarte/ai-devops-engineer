# Demo

The fastest demonstration is the browser workflow supplied by the runtime.

```bash
cd 35_RUNTIME
docker compose up --build
```

Open `http://localhost:8080/` and click **Create demo incident**.

This is synthetic data. It is intentionally isolated from production infrastructure.
