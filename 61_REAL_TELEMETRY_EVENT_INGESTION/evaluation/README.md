# v3.9 Evaluation

The evaluation set uses synthetic telemetry with timestamps, tenant boundaries, and expected candidate formation. Ground truth remains outside the agent context.

Metrics:

- ingestion acceptance rate;
- normalization validity;
- candidate formation rate;
- duplicate suppression;
- cross-tenant isolation;
- provenance completeness;
- Prometheus mapping fidelity;
- OTLP mapping fidelity.
