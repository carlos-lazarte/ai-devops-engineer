# RAG + Knowledge Graph + Skills

The investigation combines three context sources:

```text
Vault lexical RAG
      +
Knowledge Graph neighborhood
      +
Skill discovery / policy eligibility
```

This is additive: graph retrieval does not override tenant isolation or policy, and skill discovery does not grant authorization.
