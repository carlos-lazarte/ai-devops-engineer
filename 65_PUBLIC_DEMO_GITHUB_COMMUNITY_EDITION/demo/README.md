# Public Demo

Run from the repository root:

```bash
./65_PUBLIC_DEMO_GITHUB_COMMUNITY_EDITION/demo/run_demo.sh
```

The script starts a temporary runtime on `127.0.0.1:18080` so it does not normally conflict with an existing development server on port 8080.
